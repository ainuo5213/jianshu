const resp = require('../module/response');
module.exports = {
    getDetail: resp.getDetail,
    getWriters: resp.getWriters,
    doLogin: resp.doLogin,
    doRegister: resp.doRegister,
    doCheckExit: resp.doCheckExit
};